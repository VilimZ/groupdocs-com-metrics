$((function(){$(window).on("scroll",(function(){var dfd;$(this).scrollTop()>50&&($(this).off("scroll"),dfd=$(".stat-number").map((function(i,el){var props={from:{count:0},to:{count:parseInt(this.dataset.n,10)}};return $(props.from).animate(props.to,{duration:3e3,step:function(now,fx){$(el).text(Math.ceil(now))},complete:function(){void 0!==el.dataset.sym&&(el.textContent=el.textContent.concat(el.dataset.sym))}}).promise()})).toArray(),$.when.apply($,dfd));var gotop = $('#backtop');$(window).scroll(function() {if ($(window).scrollTop() > 300) {gotop.addClass('show');} else {gotop.removeClass('show');}});gotop.on('click', function(e) {e.preventDefault();$('html, body').animate({scrollTop:0}, '500');});}))}));
$('.testimonial-slider').slick({
    dots: false,
    infinite: true,
    speed: 500,
    slidesToShow: 2,
    slidesToScroll: 1,
    arrows: true,
    prevArrow: $('.scrollprev'),
    nextArrow: $('.scrollnext'),
    centerPadding: '0px',
    responsive: [
    {
    breakpoint: 1024,
    settings: {
    slidesToShow: 1,
    slidesToScroll: 1,
    }
    },
    {
    breakpoint: 600,
    settings: {
    slidesToShow: 1,
    slidesToScroll: 1
    }
    },
    {
    breakpoint: 480,
    settings: {
    slidesToShow: 1,
    slidesToScroll: 1,
    focusOnSelect: true,
    }
    }
    ]
    });
    $('a[href*="#"]')
  // Remove links that don't actually link to anything
  .not('[href="#"]')
  .not('a.nav-link')
  .not('[href="#0"]')
  .click(function(event) {
    // On-page links
    if (
      location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') 
      && 
      location.hostname == this.hostname
    ) {
      // Figure out element to scroll to
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
      // Does a scroll target exist?
      if (target.length) {
        // Only prevent default if animation is actually gonna happen
        event.preventDefault();
        $('html, body').animate({
          scrollTop: target.offset().top
        }, 1000, function() {
          // Callback after animation
          // Must change focus!
          var $target = $(target);
          $target.focus();
          if ($target.is(":focus")) { // Checking if the target was focused
            return false;
          } else {
            $target.attr('tabindex','-1'); // Adding tabindex for elements not focusable
            $target.focus(); // Set focus again
          };
        });
      }
    }
  });
